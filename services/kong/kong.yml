_format_version: "1.1"
services:
- host: "backend"
  name: my-backend
  port: 8005
  protocol: http
  routes:
  - name: backend
    paths:
    - /api
    strip_path: false

plugins:
- name: bot-detection